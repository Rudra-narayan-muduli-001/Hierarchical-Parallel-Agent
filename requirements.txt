# Core
pydantic>=2.5.0
fastapi>=0.104.0
uvicorn>=0.24.0
pyyaml>=6.0
pytest>=7.4.0
httpx>=0.25.0
websockets>=12.0

# MCP Protocol (InfoSeeker integration — worker pools)
mcp>=1.20.0
fastmcp>=2.0.0

# Optional: LangChain MCP adapters (for direct InfoSeeker agent compatibility)
langchain-mcp-adapters>=0.1.0
